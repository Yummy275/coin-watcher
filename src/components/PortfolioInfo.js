import React from 'react';

const styles = {
    mainContainer: '',
};

const PortfolioInfo = () => {
    return (
        <div className={styles.mainContainer}>
            <p>HELLO</p>
        </div>
    );
};

export default PortfolioInfo;
